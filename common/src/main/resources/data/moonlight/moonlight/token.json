{
  "comment": "if a server pack resource manager contains this file in this location it will trigger dynamic pack assets generation. Better than to check if both loader packs are loaded as that still could not guarantee that they arent empty"
}