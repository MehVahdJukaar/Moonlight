
modLoader="javafml" #mandatory

loaderVersion="[36,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.

license="All rights reserved"

[[mods]] #mandatory

modId="selene" #mandatory

version="${file.jarVersion}" #mandatory

displayName="Selene" #mandatory

logoFile="icon.png" #optional

itemIcon="minecraft:end_stone" #catalogue icon
credits="" #optional

authors="MehVahdJukaar" #optional

description='''
A small library that allows to make custom map markers, first and third person animations and contains the soft fluid system as well as other utilities
'''

[[dependencies.selene]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[36,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.selene]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.16.3,1.17)"
    ordering="NONE"
    side="BOTH"
